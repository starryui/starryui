{
 "name": "@starryui/starryui-docs",
 "version": "0.0.1",
 "types": "dist/index.d.ts",
 "description": "",
 "main": "dist/index.js",
 "scripts": {
  "compile": "rm -rf dist/* && tsc -noEmit -p tsconfig.pkg.json && esbuild index.ts --bundle --outfile=dist/main.js",
  "compile-watch": "rm -rf dist/* && esbuild index.ts --watch --bundle --outfile=dist/main.js"
 },
 "keywords": [],
 "author": "",
 "license": "ISC",
 "dependencies": {
  "@starryui/button": "^0.0.0",
  "@starryui/theme-midnight": "^0.0.0",
  "@starryui/theme": "^0.0.0",
  "@starryui/traits": "^0.0.0"
 }
}
